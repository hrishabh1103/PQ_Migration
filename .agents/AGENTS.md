# Workspace Rules & Preferences

- **Automatic Git Push & README Synchronization**:
  - Whenever any code or project change is made, update `README.md` to reflect the changes.
  - Automatically commit and push all changes to remote (`git add .`, `git commit -m "..."`, `git push`).
